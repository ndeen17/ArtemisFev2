# Local dev — gitignored. Vite reads this at `npm run dev`.
# /api is proxied to http://localhost:4000 by vite.config.ts, so just keep it relative.
VITE_API_BASE_URL=/api

# Google Identity Services client ID (must match GOOGLE_CLIENT_ID in BE .env).
VITE_GOOGLE_CLIENT_ID=654807456151-hnu87ifmqcpj5g2kq1vqfhioacc2jn1u.apps.googleusercontent.com
